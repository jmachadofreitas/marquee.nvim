# marquee.nvim

`marquee` is a small Neovim marks helper.

It focuses on:

- showing primary marks with signs
- filtering `Telescope` and `which-key` mark views to primary marks
- quick mark deletion helpers

